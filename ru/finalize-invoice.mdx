---
title: "Финализация инвойса"
---

Устанавливает `status='issued'`, `issued_at=NOW()`, `due_at=NOW()+30 days`.

После выпуска документ считается юридически значимым. Изменять его нельзя, допускается только `void` или `credit note`.
