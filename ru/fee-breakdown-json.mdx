---
title: "JSON-структура breakdown комиссий"
---

```text
{
  "pct": 2.0,
  "flat": 0.25,
  "min": 0.25,
  "applied_min": false
}
```

Для записей `breakage` используется формат: `{ "note": "expired_unfunded_return_pending" }`
