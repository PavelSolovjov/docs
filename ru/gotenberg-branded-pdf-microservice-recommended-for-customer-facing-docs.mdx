---
title: "Gotenberg — брендированный PDF-микросервис, рекомендован для customer-facing документов"
---

**GitHub**: [https://github.com/gotenberg/gotenberg](https://github.com/gotenberg/gotenberg)\
**License**: MIT\
**Docker image**: `gotenberg/gotenberg:8`

Принимает HTML через HTTP POST и возвращает PDF. Node.js не требуется, это чистый Docker-контейнер.

```text
const form = new FormData();
form.append("files", new Blob([html], { type: "text/html" }), "index.html");
const res = await fetch("http://gotenberg:3000/forms/chromium/convert/html", {
  method: "POST", body: form,
});
const pdf = await res.arrayBuffer();
```

Использовать для: брендированных customer-facing settlement statements с логотипами, QR-кодами и кастомной типографикой.
