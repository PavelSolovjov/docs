---
title: "Retry Infrastructure"
---

`collection_attempts` (INT) and `next_retry_at` (TIMESTAMPTZ) columns exist (migration 035) as scaffolding for a future automated retry queue. No retry job is implemented yet — see §14 roadmap.